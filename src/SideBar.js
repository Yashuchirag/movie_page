import './App.css';

const SideBar = () => {

    return (
        <aside className="sidebar">
            <h2>Movie Selection Criteria</h2>
            <p>Add your custom details here (links, notes, categories, etc.)</p>
        </aside>
    );
};

export default SideBar;